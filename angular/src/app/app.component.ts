import {Component} from '@angular/core';

@Component({
    selector: 'app-root',
    template: 'Angular app working!'
})
export class AppComponent {
    constructor() {
        console.log('AppComponent booted');
    }
}